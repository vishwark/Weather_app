import 'package:flutter/widgets.dart';
import 'package:weather_app/widgets/favorites.dart';

class Favorites extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const FavoritesList(); // Use FavoritesList or any other widget you intend to display
  }
}
